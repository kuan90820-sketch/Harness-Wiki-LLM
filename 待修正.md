# 修改計畫：邏輯本體搬進 Skills，Commands 只負責呼叫

## 決策

將三大操作（ingest / query / lint）的完整 SOP 從 `prompts/` 搬進 `.claude/skills/`。
Commands 變成「只確保 skill 被載入執行」的薄殼。

---

## 整體架構變化

```
修改前：
.claude/commands/ingest.md ──→ prompts/ingest.md（邏輯本體）
.agent/workflows/ingest.md ──→ prompts/ingest.md（邏輯本體）

修改後：
.claude/commands/ingest.md ──→ .claude/skills/ingest/SKILL.md（邏輯本體）
.agent/workflows/ingest.md ──→ prompts/ingest.md（薄殼）──→ 說明邏輯已移至 skills
```

---

## 逐檔說明

### 新建（3 個）

| 路徑 | 內容 |
|------|------|
| `.claude/skills/ingest/SKILL.md` | 完整 ingest SOP（從 `prompts/ingest.md` 搬來），加 YAML frontmatter |
| `.claude/skills/query/SKILL.md` | 完整 query SOP（從 `prompts/query.md` 搬來），加 YAML frontmatter |
| `.claude/skills/lint/SKILL.md` | 完整 lint SOP（從 `prompts/lint.md` 搬來），加 YAML frontmatter |

SKILL.md frontmatter 格式（以 ingest 為例）：
```yaml
---
name: ingest
description: 把一份新 source 整合進 wiki。觸發時機：使用者說「處理 raw/X」「ingest X」或明確要整合新資料時。
---
```

---

### 改寫（3 個）：`.claude/commands/`

Commands 不再指向 `prompts/`，改為直接宣告「執行對應 skill」。

**`.claude/commands/ingest.md` 改後：**
```markdown
---
description: 把一份新 source 整合進 wiki — 執行 ingest skill（讀 → 對齊 → 寫 summary → 跨頁更新 → 更新 index/log → 機械化檢查 → 回報）
---

# /ingest — 把新 source 整合進 wiki

執行 **ingest skill**，完整走完步驟 1–7。

## 使用方式

/ingest raw/<檔名>

## 你（agent）的動作

1. 讀根目錄的 AGENTS.md 與 CLAUDE.md 喚起 wiki 公約。
2. 載入並執行 ingest skill 的完整 SOP。
3. 不可省略「步驟 2：與使用者快速對齊」。
4. 如果使用者沒附檔名，先問再開工。
```

query / lint 同理，改為「執行 query skill」「執行 lint skill」。

---

### 改寫（3 個）：`prompts/`

`prompts/` 從邏輯本體降級為薄殼，保留給 Antigravity（`.agent/workflows/` 仍指向這裡）與 CLAUDE.md 的舊引用。

**`prompts/ingest.md` 改後：**
```markdown
# Ingest 工作流

> ⚠️ 邏輯本體已搬至 `.claude/skills/ingest/SKILL.md`。
> 這份檔案保留供非 Claude Code 工具（如 Antigravity）參考。

完整 SOP 請讀 `.claude/skills/ingest/SKILL.md`。
```

query / lint 同理。

---

### 不動（維持現狀）

| 路徑 | 原因 |
|------|------|
| `.agent/workflows/*.md` | 繼續指向 `prompts/`（prompts 變薄殼後會說明邏輯去了哪） |
| `wiki/` 全部內容 | 與此次重構無關 |
| `scripts/check-consistency.sh` | 不受影響 |
| `.githooks/pre-commit` | 不受影響 |
| `.github/workflows/consistency.yml` | 不受影響 |

---

### 需同步更新

| 路徑 | 變更內容 |
|------|---------|
| `CLAUDE.md` | 表格中的 SOP 連結從 `prompts/ingest.md` 改為 `.claude/skills/ingest/SKILL.md`（三行） |
| `架構.md` | 第五、六節的說明更新為 skills 是邏輯本體，prompts 是薄殼 |
| `待修正.md` | 完成後刪除或清空 |

---

## 執行順序

1. 建立三個 skill 目錄與 `SKILL.md`（搬邏輯）
2. 改寫三個 `.claude/commands/*.md`（呼叫 skill）
3. 改寫三個 `prompts/*.md`（降為薄殼）
4. 更新 `CLAUDE.md` 連結
5. 更新 `架構.md` 說明

---

## 注意事項

- `.agent/workflows/` 的 Antigravity 工具透過 `prompts/`（薄殼）間接得知邏輯位置，但無法直接讀 `.claude/skills/`。這是接受的取捨——若未來需要讓 Antigravity 也有完整能力，需在 `.agent/workflows/` 補完整邏輯（目前不做）。
- `CLAUDE.md` 內「漸進式披露」段的連結要一併改，否則使用者點進去會到舊薄殼而非完整 SOP。

---

---

# 修改計畫：子規定 AGENTS.md 搬進 `.claude/Docs/`

## 決策

將所有子目錄的 `AGENTS.md`（wiki 寫作規則、模板）統一移至 `.claude/Docs/`。
根目錄的 `AGENTS.md` 保留不動（給 Codex / Antigravity 用的薄殼）。

---

## 搬遷對照表

| 原路徑 | 新路徑 |
|--------|--------|
| `prompts/AGENTS.md` | `.claude/Docs/prompts-conventions.md` |
| `raw/AGENTS.md` | `.claude/Docs/raw-conventions.md` |
| `wiki/AGENTS.md` | `.claude/Docs/wiki-conventions.md` |
| `wiki/entities/AGENTS.md` | `.claude/Docs/wiki-entities.md` |
| `wiki/concepts/AGENTS.md` | `.claude/Docs/wiki-concepts.md` |
| `wiki/summaries/AGENTS.md` | `.claude/Docs/wiki-summaries.md` |
| `wiki/syntheses/AGENTS.md` | `.claude/Docs/wiki-syntheses.md` |

---

## 前導引文件：需同步更新的路徑引用

### `CLAUDE.md`
- 第 4 行：「詳細寫作公約寫在每個子目錄自己的 `AGENTS.md`」→ 改為「`.claude/Docs/`」
- 第 12 行：「各目錄的 `AGENTS.md`」→ 改為「`.claude/Docs/` 下各規定檔」
- 第 35 行：`[wiki/AGENTS.md](wiki/AGENTS.md)` → `[.claude/Docs/wiki-conventions.md](.claude/Docs/wiki-conventions.md)`
- 第 57 行：「細節放到子目錄 `AGENTS.md`」→ 改為「`.claude/Docs/`」

### `prompts/ingest.md`
- 第 19 行：`wiki/AGENTS.md` → `.claude/Docs/wiki-conventions.md`
- 第 33 行：`wiki/summaries/AGENTS.md` → `.claude/Docs/wiki-summaries.md`

### `prompts/query.md`
- 第 46 行：`wiki/syntheses/AGENTS.md` → `.claude/Docs/wiki-syntheses.md`

### `.claude/Docs/wiki-conventions.md`（搬移後的內容）
- 「下一步」段的四個連結：`entities/AGENTS.md`、`concepts/AGENTS.md`、`summaries/AGENTS.md`、`syntheses/AGENTS.md` → 改為對應的 `.claude/Docs/wiki-*.md` 路徑

### `README.md`
- 所有提到子目錄 `AGENTS.md` 存放位置的描述段落，更新為 `.claude/Docs/`

### `架構.md`
- 目錄結構圖中的 `AGENTS.md` 項目（prompts/、raw/、wiki/ 各子目錄）→ 更新為 `.claude/Docs/` 路徑
- 第 253、263、321、330、342、355 行附近的說明文字

---

## 不需修改的檔案

| 路徑 | 原因 |
|------|------|
| `AGENTS.md`（根目錄） | 給 Codex / Antigravity，維持原慣例 |
| `.claude/commands/*.md` | 引用的是根目錄 `AGENTS.md`，不受影響 |
| `.agent/workflows/*.md` | 同上 |
| `prompts/lint.md` | 僅提到「不含各 AGENTS.md」的孤兒頁排除邏輯，需確認是否要改為 `.claude/Docs/` |

---

## 執行順序

1. 建立 `.claude/Docs/` 目錄，複製七個 AGENTS.md 內容並重命名
2. 更新 `.claude/Docs/wiki-conventions.md` 內部的「下一步」連結
3. 更新 `CLAUDE.md`（四處）
4. 更新 `prompts/ingest.md`（兩處）
5. 更新 `prompts/query.md`（一處）
6. 更新 `README.md`
7. 更新 `架構.md`
8. 刪除原七個子目錄的 `AGENTS.md`
9. 跑 `bash scripts/check-consistency.sh` 確認無斷鏈

---

## 注意事項

- `scripts/check-consistency.sh` 的 W3（內部連結檢查）會抓到斷鏈，步驟 9 是最終驗證。
- `prompts/lint.md` 的孤兒頁排除邏輯若有「不含 AGENTS.md」字樣，需一併確認是否仍適用。
