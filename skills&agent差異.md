# Skills、AGENTS.md／CLAUDE.md 與 Workflows 完全入門

> 這份文件寫給**第一次聽到這些名詞、也沒有 AI 背景的人**。
> 目的是讓你看完之後，能理解「為什麼同一個 AI（我）在不同的資料夾、不同的情境會有不同的反應」。

---

## 0. 先建立一個直覺：AI 不是固定的

你大概會以為「Claude（也就是我）」是一個寫死的程式，每次回答的方式都一樣。

實際上不是。

每一次你開啟一個新的對話，我都像是一個「**剛上工、什麼都不知道的新員工**」。我會在開始工作之前，先讀一疊「**交接文件**」，這些文件決定了：

- 我自稱是誰、處理什麼樣的專案
- 我該遵守哪些規矩、不該做什麼事
- 遇到什麼情境該採取哪種 SOP
- 我有哪些「特殊技能」可以呼叫

**這疊交接文件就是 AGENTS.md（或 CLAUDE.md）與 Skills。**

理解這兩個東西，就等於拿到了「**調教這個 AI**」的方向盤。

---

## 1. AGENTS.md / CLAUDE.md ── 永遠攤在桌上的員工手冊

### 1.1 它是什麼

`AGENTS.md`（或我這邊用的 `CLAUDE.md`）是一份放在資料夾裡的**純文字檔**（markdown 格式，就是有 `#` 標題的那種文件）。

它的特性是：

> **只要我進入這個資料夾工作，這份檔案的內容就會「自動、永久」攤在我的桌上。**

我不需要去翻找，也不能裝作沒看到。它就在那。

### 1.2 用「員工手冊」來比喻

想像你開了一家咖啡店，僱了一個臨時工。你不可能每天現場一條一條告訴他：
- 「我們的咖啡豆要冰在冷藏 4 度」
- 「客人說『微糖』是指 3 公克」
- 「絕對不要動老闆桌上那台手沖壺」

於是你寫了一本《員工手冊》貼在工作檯旁邊。每個新進來的員工，第一件事就是讀這本手冊。

**`AGENTS.md` 就是這本手冊。** 每一個進到這個專案資料夾工作的 AI，第一件事就是讀它。

### 1.3 它的「層級」

員工手冊不只一本，會依照範圍疊起來：

| 層級 | 放在哪 | 適用範圍 | 比喻 |
|---|---|---|---|
| **全域使用者規則** | `C:\Users\你的帳號\.claude\CLAUDE.md` | 你電腦上**所有的專案** | 你個人的工作習慣總則（例：所有專案都要用繁體中文回我） |
| **專案規則** | 專案資料夾裡的 `CLAUDE.md` | 只在**這個專案**生效 | 這家店特有的 SOP（例：本店牛奶用燕麥奶不用牛奶） |
| **個人專案規則** | 專案資料夾裡的 `CLAUDE.local.md` | 同上，但**不會被同事看到** | 你貼在抽屜裡的私人備忘錄（不會被 git 上傳） |

**疊加的順序是：下層可以覆蓋上層。** 例如你在全域規則寫「不要用 emoji」，但某個專案的 `CLAUDE.md` 寫「這個專案可以用 emoji」，那在那個專案裡，我就會用 emoji。

### 1.4 實際長什麼樣（拿你這個專案來舉例）

你現在所在的這個資料夾 `d:\Harness-Wiki-LLM\` 裡面就有一份 `CLAUDE.md`，它大致告訴我：

- 「你是 `{{TOPIC}}` 這個個人知識庫的維護者」 ← **這定義了我的角色**
- 「`raw/` 資料夾只能讀，不能改」 ← **這是不可違反的紅線**
- 「使用者說『處理 raw/X』時，要去執行某個 SOP」 ← **這是觸發語對應表**
- 「每次更新 wiki 頁，順手更新 index 和 log」 ← **這是工作流程**

換句話說，因為這份檔案的存在，我才會在這個資料夾裡表現得「**像一個 wiki 管理員**」。如果你把同樣的我搬到別的資料夾、沒有這份檔案，我就只是一個普通的程式助理。

### 1.5 它的成本：每次都會被讀

員工手冊有個缺點 ── **它一直攤在桌上**。

對 AI 來說，桌面（叫做「上下文視窗」）是有大小限制的。員工手冊越厚，能容納其他資訊（例如你的程式碼、你的問題）的空間就越少。

所以 `AGENTS.md` 的寫作原則是：
- **精簡** ── 只放真正每次都要用到的核心規則
- **指路** ── 詳細的細節放到其他檔案，員工手冊只負責「指路」（叫做「漸進式披露」）

你會在 `CLAUDE.md` 的結尾看到一段「下一步往哪看」，就是這個原理 ── 把細節推到別的檔案，需要時再去翻。

---

## 2. Skills ── 工具箱裡的「專用工具」

### 2.1 它是什麼

Skills 是一個一個「**模組化的專長**」，每個 skill 是一個資料夾，裡面通常有：
- 一份 `SKILL.md`（這個技能的說明書）
- 可能還有腳本、範本、參考資料

跟 AGENTS.md 不同的是：

> **Skills 不會自動攤在我桌上。它們躺在工具箱裡，只有在需要的時候才會被拿出來。**

### 2.2 用「廚師的專用刀具」來比喻

想像一個專業廚師，他的料理檯上永遠放著基本工具（鍋、鏟、菜刀），這些是 `AGENTS.md`。

但他的櫃子裡還有一堆**專用刀具**：
- 處理魚的時候才會拿出來的「片魚刀」
- 切壽司才用的「柳刃」
- 剁骨頭才用的「中華菜刀」

這些就是 **Skills**。

廚師不會把所有刀都同時擺在檯面上 ── 那很佔位子、看起來也很亂。他會在點單進來的瞬間判斷：「這是一道生魚片，我需要柳刃。」然後才把它拿出來。

### 2.3 我怎麼知道什麼時候該拿哪一把刀

每個 skill 都有一張**標籤**，標籤上寫著「我是處理什麼東西的」。

例如你這個專案有三個 skill：

| Skill 名稱 | 標籤上寫的觸發條件 |
|---|---|
| `ingest` | 使用者說「處理 raw/X」「ingest X」或要整合新資料進 wiki 時 |
| `lint` | 使用者說「lint wiki」「健康檢查」時 |
| `query` | 使用者說「問 …」「查 …」「回答 …」時 |

當你輸入訊息時，我會掃過所有 skill 的標籤，看看你的話有沒有對應到某個 skill。如果有，我就會「**拿出那把刀**」── 也就是去讀那個 skill 的完整 SKILL.md，然後照裡面的 SOP 工作。

如果沒對應到任何 skill，我就用基本工具處理。

### 2.4 為什麼要這樣設計？

兩個原因：

1. **省桌面空間**：如果把所有專長的 SOP 都寫進 `AGENTS.md`，那份手冊會變成一本字典，每次都要讀，太耗。
2. **可組合**：你可以裝很多 skill，但每次對話只啟動相關的那一兩個。就像廚師的刀櫃裡有 50 把刀，但一道菜只用到 2 把。

### 2.5 Skills 從哪來

- **你自己寫的**：放在專案的 `.claude/skills/<名字>/SKILL.md`，例如你的 `ingest`、`lint`、`query`
- **內建或外掛的**：Claude Code 自己附帶了一些（例如 `pdf`、`docx`、`xlsx` 用來處理對應檔案），還有 plugin 安裝的（例如 `codex:rescue`）
- **共用的**：可以放在使用者層級的 `~/.claude/skills/`，跨所有專案都能用

---

## 3. Workflows ── Antigravity 特有的「標準作業程序（SOP）」與快捷指令

### 3.1 它是什麼

Workflows 是一份 Markdown 格式的「**步驟指南**」，提供給 AI（尤其是 Antigravity）在執行特定任務時，必須一步步遵守的詳細指令。

> **如果 Skills 決定了「我會做什麼」，Workflows 則嚴格規定了「我該按照什麼順序把這件事做好」。**

### 3.2 用「餐廳的標準食譜與點菜單」來比喻

如果 `AGENTS.md` 是基本廚具，`Skills` 是抽屜裡的專用菜刀，那麼 `Workflows` 就是貼在牆上的**標準食譜（SOP）**。

當你下達某個特定指令時，我會去把這張食譜拿下來，然後嚴格按照「第一步備料、第二步熱鍋、第三步下料」的順序執行。

### 3.3 它的觸發方式：斜線指令（Slash Commands）

你通常可以透過打出「斜線指令」來直接呼叫一個 Workflow。
例如你在對話框輸入 `/ingest`，Antigravity 就會去尋找對應的 Workflow 檔案並開始執行。

### 3.4 三層轉介架構（以這個專案為例）

在這個專案中，因為我們希望同時支援不同的 AI 工具（如 Claude Code 與 Google Antigravity），所以 Workflows 被設計成了「三層架構」：

| Slash 指令 | 入口薄殼（Antigravity 找這個） | 轉介薄殼 | 真正邏輯（本體，也就是 Skill） |
|---|---|---|---|
| `/ingest` | `.agent/workflows/ingest.md` | `prompts/ingest.md` | `.claude/skills/ingest/SKILL.md` |
| `/query` | `.agent/workflows/query.md` | `prompts/query.md` | `.claude/skills/query/SKILL.md` |
| `/lint` | `.agent/workflows/lint.md` | `prompts/lint.md` | `.claude/skills/lint/SKILL.md` |

- **Antigravity** 會看 `.agent/workflows/<name>.md`
- **Claude Code** 會看 `.claude/skills/<name>/SKILL.md`
- 當你要**修改具體的執行步驟時，永遠去改最後面的 `SKILL.md`（邏輯本體）**，前面的「薄殼」只是負責把 AI 導向正確的地方。

### 3.5 自動化標籤：`// turbo` 與 `// turbo-all`

Workflows 還有一個很強大的功能：**授權自動執行指令**。
如果你在 Workflow 的步驟上方加上 `// turbo` 標註，那一步如果需要跑終端機指令（例如 git commit），我就會**自動幫你跑，不用等你按同意**。
如果在檔案任何地方加上 `// turbo-all`，則整個 Workflow 裡的所有指令都會自動執行。這對於已經非常成熟、不會出錯的自動化流程非常有用。

---

## 4. 三者並列比較（最關鍵的一張表）

| 面向 | **AGENTS.md / CLAUDE.md** | **Skills** | **Workflows** |
|---|---|---|---|
| 比喻 | 員工手冊 | 工具箱裡的專用刀 | 牆上的標準食譜與點菜單 |
| 載入方式 | **自動、永遠** | **被動、按需** | **被動、明確觸發** |
| 觸發條件 | 進入這個資料夾就讀 | 使用者的話對到 skill 描述才啟動 | 使用者打 `/指令` 或明顯相關才啟動 |
| 內容性質 | 「我是誰、我遵守什麼原則」 | 「處理某個領域的專長」 | 「一步一步執行的 SOP 與指令」 |
| 大小限制 | 要精簡（佔桌面空間） | 可以很長很詳細 | 按步驟寫清楚即可 |
| 範圍 | 全域 / 專案 / 個人三層 | 全域 / 專案 / plugin 三層 | 專案層級的 `.agent/workflows/` |
| 改了之後 | **下次對話**就會生效 | **下次匹配到觸發語**才生效 | **下次呼叫 workflow**才生效 |
| 適合放什麼 | 角色定位、紅線、不變的工作流程 | 特定任務的詳細知識與範本 | 明確的步驟 1、2、3 以及 `// turbo` 標籤 |

### 4.1 一句話總結

> **`AGENTS.md` 決定我「是什麼樣的人」，Skills 決定我「會哪些專長」，Workflows 決定我「照什麼明確步驟做事」。**

---

## 5. 一次完整對話的內部流程（示意）

假設你打開 `d:\Harness-Wiki-LLM\` 跟我說：「**/ingest raw/2026-financial-report.pdf**」或「**幫我處理 raw/...**」：

我的內部處理順序大致是這樣：

```text
1. 啟動
   └─ 讀全域 CLAUDE.md（C:\Users\yuzho\.claude\CLAUDE.md）
       └─ 學到：計劃書放 plans/、用中文檔名、commit 規則...

2. 進入專案資料夾
   └─ 讀專案 CLAUDE.md（d:\Harness-Wiki-LLM\CLAUDE.md）與 AGENTS.md
       └─ 學到：我是 wiki 維護者、raw/ 不能改、三大觸發語...

3. 收到你的指令「/ingest raw/2026-financial-report.pdf」
   └─ 發現你呼叫了 Slash 指令，去尋找 Workflow
       └─ 找到 `.agent/workflows/ingest.md`（入口薄殼）
       └─ 根據指示，轉介去讀取 `prompts/ingest.md`，最後讀取 `.claude/skills/ingest/SKILL.md`（邏輯本體）
           └─ 學到：先讀 source → 寫 summary → 跨頁更新 → 檢查

4. 開始照 Workflow SOP 工作
   └─ 整個過程中，CLAUDE.md 的紅線（raw/ 不能改）持續生效
   └─ 如果 SOP 裡面有標註 `// turbo` 的執行步驟，我就會不問你直接幫你自動跑完！
```

**重點**：`AGENTS.md` 從頭到尾都在「桌上」，Skill/Workflow 只在被觸發後才加入。彼此是疊加、不是互斥。

---

## 6. 對你（使用者）來說，這代表什麼？

理解這套機制之後，你會發現自己有幾種「**遙控**」我的方式：

### 6.1 想讓我「永遠記得某件事」 → 改 `AGENTS.md`

例：你受夠了每次都要提醒我「回答用繁體中文」。
→ 把這條寫進 `~/.claude/CLAUDE.md`，以後所有對話都自動生效。

### 6.2 想讓我「遇到某事時拿出特定專長」 → 寫一個 Skill

例：你常常要把訪談錄音轉成結構化的 wiki 頁。
→ 寫一個 `.claude/skills/interview-to-wiki/SKILL.md`，並設定語意觸發條件。

### 6.3 想讓我「按照固定步驟且全自動執行」 → 寫一個 Workflow

例：你有一套發布前的自動檢查跟打包流程。
→ 寫一個 `.agent/workflows/deploy.md`，裡面列出步驟，並加上 `// turbo-all`。以後只要打 `/deploy`，我就會劈哩啪啦一路自動跑完。

### 6.4 想偵錯「為什麼 AI 又忘記/又出錯」

兩個地方去翻：
1. **`AGENTS.md` 是不是漏寫了那條規則？** 或者寫得太模糊讓我誤解？
2. **是不是某個 Skill/Workflow 裡的步驟寫得不夠精準**，導致我做錯？

### 6.5 想讓我「忘記」某個習慣

兩種可能：
- 如果是寫在 `AGENTS.md` 裡的 → **直接刪那一行**
- 如果是某個 Workflow 自動觸發造成的 → **改觸發條件**，或乾脆刪掉那個檔案。

---

## 7. 三條給新手的提醒

1. **`AGENTS.md` 不是越多越好，是越精準越好**。塞太多會吃掉 AI 的工作空間，也讓重要規則被淹沒。寧可放 20 條核心，把詳細的 SOP 推到 Workflow 和 Skill 裡。

2. **Workflow 和 Skill 的觸發條件很關鍵**。不論是 Skill 裡的 Description，或是 Workflow 的名稱（`/名稱`），這決定了工具會不會被正確拿出來用。

3. **改完之後要新開一個對話才會完全生效**。同一個對話中，已經讀進「桌面」的舊版本可能會繼續用，直到你重新開始。

---

## 附錄：你這個專案目前的配置長什麼樣

```text
d:\Harness-Wiki-LLM\
├── CLAUDE.md                          ← 員工手冊（專案層級）
├── AGENTS.md                          ← Antigravity 專用的員工手冊（裡面指路到 CLAUDE.md）
├── .agent\
│   └── workflows\                     ← Antigravity 的斜線指令入口薄殼
│       ├── ingest.md                  ← `/ingest` 會觸發這個
│       ├── lint.md                    ← `/lint` 會觸發這個
│       └── query.md                   ← `/query` 會觸發這個
├── prompts\                           ← 轉介薄殼
├── .claude\
│   ├── Docs\                          ← 員工手冊指過去的細則
│   │   ├── wiki-conventions.md
│   │   └── ...
│   └── skills\                        ← 你的工具箱（也是 Workflow 的邏輯本體）
│       ├── ingest\SKILL.md            ← 「處理 raw/X」會觸發 / 也是 ingest 的真實 SOP
│       ├── lint\SKILL.md              ← 「lint wiki」會觸發 / 也是 lint 的真實 SOP
│       ├── query\SKILL.md             ← 「問 …」「查 …」會觸發 / 也是 query 的真實 SOP
│       └── karpathy-guidelines\       ← 寫程式時的行為準則
├── raw\                               ← 你的原始資料（我只能讀）
└── wiki\                              ← 我負責整理的成品

C:\Users\yuzho\.claude\
└── CLAUDE.md                          ← 員工手冊（全域層級，所有專案共用）
```

看懂這張圖，你就掌握了「為什麼我在這個資料夾會表現成 wiki 管理員，而且打 `/ingest` 就能自動跑一整套流程」的全部秘密。
