# Harness-Wiki-LLM — 專案架構分析

> 給未來的我（或新加入的 AI agent）一份地圖：這個專案的每個資料夾在做什麼、檔案之間如何串。
>
> 想看「為什麼設計成這樣」的背景思考，去讀 [README.md](README.md) 與 [Andrej Karpathy LLM Wiki 架構.md](Andrej%20Karpathy%20LLM%20Wiki%20%E6%9E%B6%E6%A7%8B.md)。
> 想看寫作公約細節，去讀 `.claude/Docs/` 下對應規定檔。
>
> 最後更新：2026-05-14

---

## 零、新手快速地圖（沒有程式背景也能讀）

> 這一節是給第一次接觸這個模板的人。如果你已熟悉 Git 與 CI/CD，可以直接跳到第一節。

### 0-1 五個核心資料夾一眼看懂

```
你的知識庫專案
│
├── 📥 raw/          ← 你丟進來的原始資料（只讀，永不修改）
│
├── 📖 wiki/         ← AI 整理好的知識頁面（成果區）
│
├── 🤖 .claude/skills/  ← 告訴 AI「怎麼工作」的說明書（prompts/ 是它的工具中立薄殼）
│
├── 🛡️ .githooks/    ← 存檔前的「品質守門員」（在你電腦上跑）
│
└── ☁️ .github/      ← 上傳雲端後的「品質守門員」（在 GitHub 跑）
```

---

### 0-2 `raw/` — 你的原始資料倉庫

**比喻：** 就像你的抽屜，你把找到的資料、文章、筆記全部丟進來。

**使用方式：**
1. 把你想整理的素材（文章、PDF 轉文字、書摘、訪談記錄）直接放進 `raw/`
2. 對 AI 說：「處理 `raw/xxx.md`」或輸入 `/ingest raw/xxx.md`
3. AI 會讀取它並開始整理，你**不需要自己動手寫任何摘要**

**帶來的效果：**
- 原始資料永遠保留、永不被 AI 修改——你可以反覆讓 AI 用同一份資料做不同角度的分析
- AI 整理有「根據」，不會憑空捏造

**適合場景：**
- 你讀完一篇文章或一本書，想讓 AI 幫你整理重點
- 你有一堆舊筆記散落各處，想統一整合進知識庫
- 你想追蹤某個主題隨時間的演變（每次新增一份 source 就 ingest 一次）

---

### 0-3 `wiki/` — AI 整理後的知識庫

**比喻：** 就像整理好的百科全書，按主題分類排好。

```
wiki/
├── summaries/   ← 每份原始資料的「濃縮摘要」（與 raw/ 一對一）
├── entities/    ← 人物、組織、產品的專屬頁面
├── concepts/    ← 抽象術語、理論、方法的解釋頁
├── syntheses/   ← AI 跨多份資料綜合分析的頁面
├── index.md     ← 所有頁面的總目錄（像書的目錄）
└── log.md       ← 每次修改的紀錄（像工作日誌）
```

**使用方式：**
- **讀**：你可以直接打開 `wiki/index.md` 瀏覽所有已整理的頁面
- **問**：對 AI 說「問 XXX」，AI 會先讀 index，再讀相關頁，然後綜合回答你，並附上引用來源
- **檢查**：你隨時可以翻開任何 wiki 頁確認 AI 說的有沒有根據

**帶來的效果：**
- 知識有系統地連結：人名、術語、主題互相交叉引用，不再是孤立的筆記
- 每次問問題，AI 都會從你自己整理過的資料裡找答案，不是胡說

**適合場景：**
- 你在研究某個主題（如 AI、健康、投資），資料越積越多，需要一個中心整理點
- 你想快速回憶三個月前讀過的某本書的重點
- 你想讓 AI 幫你比較兩份資料的異同（AI 會讀兩份摘要再綜合回答）

---

### 0-4 `.claude/skills/` — AI 的工作說明書（邏輯本體）

**比喻：** 就像你給新員工的「標準作業程序手冊」，告訴 AI 遇到不同任務時，每一步要怎麼做。

```
.claude/skills/
├── ingest/SKILL.md  ← 「新資料進來時」的完整步驟（共 7 步）
├── query/SKILL.md   ← 「回答問題時」的完整步驟（共 5 步）
└── lint/SKILL.md    ← 「健康檢查時」的完整步驟（A/B 兩階段）

prompts/             ← 工具中立的「薄殼」，內容只是指向上面的 skill
├── ingest.md        ← 給 Antigravity 等非 Claude Code 工具讀
├── query.md
└── lint.md
```

**使用方式：**
- **你通常不需要直接讀這裡**——AI 會自動讀
- 如果你想客製化 AI 的行為（例如：希望 AI 整理摘要時一定要列出「關鍵金句」），請修改 `.claude/skills/<name>/SKILL.md`（**邏輯本體**），不是 `prompts/`（薄殼）
- 每次你輸入 `/ingest`、`/query`、`/lint`，AI 就會去讀這裡的說明書

**帶來的效果：**
- 保證每次 AI 處理資料的方式是一致的，不會因為你的問法不同而產生結構差異很大的結果
- 你的客製化只需要寫一次，之後每次觸發都自動套用

**適合場景：**
- 你想讓 AI 固定輸出某種格式（例如每份摘要一定要有「三個行動建議」）
- 你在多人協作，需要確保不同成員讓 AI 做出來的格式一致
- 你換了不同的 AI 工具（如從 Claude Code 換成 Cursor）：邏輯本體在 `.claude/skills/`，工具中立的 `prompts/` 薄殼也保留一份指向 skill，讓其他工具能透過薄殼找到本體

---

### 0-5 `.githooks/` — 本機守門員

**比喻：** 就像你在寄信前會自動檢查有沒有忘記貼郵票的小助手——不過這個助手是在你的電腦上工作。

**使用方式：**

第一步，啟用一次（只需做一次）：
```bash
git config core.hooksPath .githooks
```

之後每次你執行「儲存版本」（`git commit`）時，它會**自動跑**，不需要你記得手動觸發。

**帶來的效果：**
- 如果 wiki 目錄有檔案不一致（例如 index.md 沒更新、連結斷掉），它會**阻止你存檔**，並告訴你哪裡出問題
- 確保你的版本歷史裡每一筆 commit 都是「健康的」，不會留下一半壞掉的狀態

**適合場景：**
- 你剛讓 AI 大量 ingest 了好幾份資料，想確認整個 wiki 沒有遺漏
- 你手動修改了某個 wiki 頁，但忘記更新 index.md——守門員會提醒你
- 多人協作時，保護 wiki 不被隊友的不完整修改破壞

> **如果你完全不用 Git**，這個資料夾對你暫時沒有作用，可以先忽略。但如果你之後開始用 Git 做版本控制，記得回來啟用它。

---

### 0-6 `.github/` — 雲端守門員

**比喻：** 就像你把信寄到郵局後，郵局那邊也會再檢查一次地址有沒有填對——這道關卡在 GitHub 的伺服器上，不在你的電腦。

**使用方式：**
- **你幾乎不需要手動操作**——只要你的 repo 放在 GitHub 上，這個設定就會自動生效
- 每次你把本機的存檔「推上去」GitHub（`git push`），GitHub 會自動跑一次品質檢查
- 你可以在 GitHub 網頁上的 **Actions** 頁籤看到每次檢查的結果（綠色 ✓ = 通過，紅色 ✗ = 有問題）

**帶來的效果：**

| 情境 | `.githooks/`（本機）| `.github/`（雲端）|
|------|---------------------|-------------------|
| 你跳過本機守門員（`git commit --no-verify`）| 沒有把關 | **雲端仍會攔截** |
| 協作者的電腦沒有啟用 `.githooks/` | 沒有把關 | **雲端仍會攔截** |
| PR 合併前的最後一道關 | 不適用 | **是** |

**適合場景：**
- 你開放給別人一起編輯 wiki，需要一個「最終防線」確保合進來的修改是乾淨的
- 你想在 GitHub 的 PR 頁面看到自動化的品質報告，不需要手動跑腳本
- 你搬到新電腦還沒重新設定 `.githooks/`，雲端守門員確保這段期間不出意外

> **如果你的 repo 是私有的且只有你自己用**，`.github/` 的最大價值是幫你多一道保險，以及讓你習慣「每次 push 都有自動回饋」的工作流程。

---

### 0-7 整體流程一句話總結

```
你把資料放進 raw/
    ↓
AI 按照 .claude/skills/ 的說明書整理成 wiki/
    ↓
每次本機存檔時，.githooks/ 把關
    ↓
每次推上 GitHub 時，.github/ 再把關一次
```

**你只需要做三件事：** 丟資料進 `raw/`、對 AI 說話、偶爾翻 `wiki/` 確認結果。

---

## 一、總覽（Explorer 視角）

```
📂 Harness-Wiki-LLM/                       ← 專案根目錄
│
├── 📄 CLAUDE.md                           ← LLM 入口地圖（< 100 行）
├── 📄 AGENTS.md                           ← 跨 agent 通用規則（指回 CLAUDE.md）
├── 📄 README.md                           ← 給「人類」讀的專案說明
├── 📄 架構.md                             ← 本檔（專案架構分析圖）
├── 📄 Andrej Karpathy LLM Wiki 架構.md    ← 架構設計文件（為何長這樣）
├── 📄 教學計劃書.md                       ← 教學 / 培訓計畫書
├── 📄 Obsidian 圖片附件設定.md            ← Obsidian 圖片路徑筆記
├── 📄 Markdown範例.md                     ← Markdown 語法參考
├── 📄 Github 指令 README.md               ← 個人 git 指令備忘
├── 📄 .gitignore                          ← Git 忽略規則
│
├── 📂 .claude/                            ← Claude Code 工具的官方配置
│   ├── 📄 settings.json                   ← 權限 / 模型 / 鉤子
│   ├── 📂 commands/                       ← slash command 薄殼
│   │   ├── 📄 ingest.md                   ← /ingest → 執行 ingest skill
│   │   ├── 📄 query.md                    ← /query  → 執行 query skill
│   │   └── 📄 lint.md                     ← /lint   → 執行 lint skill
│   ├── 📂 skills/                         ← 三大操作的邏輯本體（+ 通用 skill）
│   │   ├── 📂 ingest/SKILL.md             ← 「處理 raw/X」完整 SOP（步驟 1–7）
│   │   ├── 📂 query/SKILL.md              ← 「問 / 查」完整 SOP（步驟 1–5）
│   │   ├── 📂 lint/SKILL.md               ← 「健康檢查」完整 SOP（A/B 兩階段）
│   │   └── 📂 karpathy-guidelines/SKILL.md ← Karpathy 式編碼準則
│   └── 📂 Docs/                           ← 子目錄寫作規定（原各子目錄 AGENTS.md）
│       ├── 📄 raw-conventions.md          ← raw/ 目錄規則（明確告知 LLM 不可改）
│       ├── 📄 prompts-conventions.md      ← prompts/ 慣例（現為轉介薄殼）
│       ├── 📄 wiki-conventions.md         ← wiki 寫作公約（frontmatter / 連結）
│       ├── 📄 wiki-entities.md            ← entity 頁專屬規則
│       ├── 📄 wiki-concepts.md            ← concept 頁專屬規則
│       ├── 📄 wiki-summaries.md           ← summary 頁專屬規則
│       └── 📄 wiki-syntheses.md           ← synthesis 頁專屬規則
│
├── 📂 .agent/                             ← Antigravity（或其他 agent）配置
│   └── 📂 workflows/
│       ├── 📄 ingest.md                   ← /ingest → .claude/skills/ingest/SKILL.md（必要時經 prompts/ 薄殼）
│       ├── 📄 query.md                    ← /query  → .claude/skills/query/SKILL.md
│       └── 📄 lint.md                     ← /lint   → .claude/skills/lint/SKILL.md
│
├── 📂 .github/                            ← GitHub 自動化
│   └── 📂 workflows/
│       └── 📄 consistency.yml             ← CI：跑一致性檢查（合併門）
│
├── 📂 .githooks/                          ← 本地 Git 鉤子
│   └── 📄 pre-commit                      ← commit 前自動跑一致性檢查
│
├── 📂 prompts/                            ← 工具中立薄殼（指向 .claude/skills/）
│   ├── 📄 ingest.md                       ← 薄殼 → .claude/skills/ingest/SKILL.md
│   ├── 📄 query.md                        ← 薄殼 → .claude/skills/query/SKILL.md
│   └── 📄 lint.md                         ← 薄殼 → .claude/skills/lint/SKILL.md
│
├── 📂 raw/                                ← 原始 source【只讀，永不修改；規定見 .claude/Docs/raw-conventions.md】
│   └── 📄 .gitkeep                        ← 佔位，讓空目錄能 commit
│
├── 📂 wiki/                               ← Wiki 主體【LLM 擁有並維護；規定見 .claude/Docs/wiki-*.md】
│   ├── 📄 index.md                        ← 總索引（每頁都要登記）
│   ├── 📄 log.md                          ← 更新日誌（每次改動 append 一筆）
│   │
│   ├── 📂 entities/                       ← 實體頁（人 / 組織 / 產品 / 模型）
│   ├── 📂 concepts/                       ← 概念頁（術語 / 理論 / 方法）
│   ├── 📂 summaries/                      ← 單一 source 摘要頁（與 raw/ 一對一）
│   └── 📂 syntheses/                      ← 多源綜合頁 / 問答回填 / lint 報告
│       └── 📄 open-questions.md           ← 不確定 / 矛盾 / 待補事項清單
│
└── 📂 scripts/
    └── 📄 check-consistency.sh            ← 一致性機械檢查（W1–W4）
```

---

## 二、三層職責分工（最重要的心智模型）

| 層級 | 誰擁有 | 動作 | 目錄 |
|------|--------|------|------|
| **原始層** | 使用者 | 只進不出，LLM 唯讀 | `raw/` |
| **整理層** | LLM | 讀 raw → 寫摘要 / 連結 / 綜合 | `wiki/` |
| **規則層** | 共同維護 | 規則、SOP、工具配置 | `CLAUDE.md`、`.claude/skills/`（SOP 本體）、`.claude/Docs/`（子目錄規定）、根 `AGENTS.md`、`prompts/`、`.claude/`、`.agent/` |

**口訣**：raw 是燃料、wiki 是產出、`.claude/skills/` 是說明書（`prompts/` 是工具中立的薄殼轉介）。

---

## 三、根目錄檔案逐一說明

| 檔案 | 用途 | 誰會讀 |
|------|------|--------|
| `CLAUDE.md` | LLM 進到專案時的第一份地圖：告訴模型「你是誰、你擁有什麼、有哪三大操作、不該做什麼」。必須精簡（< 100 行），細節下放到 `.claude/Docs/`。 | Claude Code（自動載入） |
| `AGENTS.md` | 跨 agent 共用的通用規則（不限於 Claude）。 | 其他 agent 工具 |
| `README.md` | 給「人類」讀的專案說明——為什麼有這個 repo、怎麼開始用。 | 人 |
| `Andrej Karpathy LLM Wiki 架構.md` | 架構設計理念的長文（這套設計為什麼長這樣、靈感來源、抉擇背後的思考）。 | 人 / 想理解設計的 AI |
| `教學計劃書.md` | 教學 / 培訓用的計畫書（給其他想學這套方法的人）。 | 人 |
| `Obsidian 圖片附件設定.md` | 在 Obsidian 裡編輯 wiki 時的圖片附件路徑設定筆記。 | 人 |
| `架構.md` | **本檔**——專案的 explorer 架構分析圖。 | 人 / 新加入的 AI |
| `.gitignore` | Git 忽略規則。 | Git |

---

## 四、`.claude/` ── Claude Code 的官方配置

**只有 Claude Code 會自動讀這個資料夾**（目錄名是工具寫死的約定）。

| 路徑 | 用途 |
|------|------|
| `.claude/settings.json` | Claude Code 的權限設定（例：允許 `Bash(gh repo *)`、`Bash(gh api *)` 不彈權限提示）。 |
| `.claude/commands/ingest.md` | **slash command 薄殼**：使用者敲 `/ingest`，Claude Code 載入這份檔案當 prompt。內容只是「請載入並執行 ingest skill」。 |
| `.claude/commands/query.md` | 同上，`/query` 的薄殼。 |
| `.claude/commands/lint.md` | 同上，`/lint` 的薄殼。 |
| `.claude/skills/ingest/SKILL.md` | **skill（邏輯本體）**：「處理 raw/X」完整 SOP（步驟 1–7）。`/ingest` command 與「ingest X」自然語觸發都會載入這個。 |
| `.claude/skills/query/SKILL.md` | **skill（邏輯本體）**：「問 / 查」完整 SOP（步驟 1–5）。 |
| `.claude/skills/lint/SKILL.md` | **skill（邏輯本體）**：「健康檢查」完整 SOP（A/B 兩階段）。 |
| `.claude/skills/karpathy-guidelines/SKILL.md` | 通用 skill：Karpathy 式編碼準則，避免常見 LLM 寫程式毛病（過度設計、非外科手術式改動、隱藏假設）。 |

> **command vs skill 的差別**：command 是使用者「敲」觸發、skill 是模型「自己判斷」觸發。  
> **本專案的設計**：command 是薄殼，skill 才是邏輯本體——使用者敲 `/ingest` 時 command 載入並執行 ingest skill；使用者自然語說「ingest X」時 skill 也會被觸發。兩條路最終跑同一份 SOP。

---

## 五、`.agent/` ── Antigravity 的入口

**結構跟 `.claude/commands/` 鏡像**，但服務不同的工具（Google Antigravity 等）。

| 路徑 | 用途 |
|------|------|
| `.agent/workflows/ingest.md` | Antigravity 的 `/ingest` 薄殼，直接指向 `.claude/skills/ingest/SKILL.md`；若工具讀不到該路徑，可改讀 `prompts/ingest.md` 轉介薄殼。 |
| `.agent/workflows/query.md` | Antigravity 的 `/query` 薄殼。 |
| `.agent/workflows/lint.md` | Antigravity 的 `/lint` 薄殼。 |

### 為什麼要分兩個資料夾？

| 目的 | 位置 | 為什麼 |
|------|------|--------|
| 邏輯本體 | `.claude/skills/<name>/SKILL.md` | Claude Code 的 skill 格式，能被 `/<name>` command 與自然語觸發載入 |
| 工具中立薄殼 | `prompts/` | 給非 Claude Code 工具（Antigravity 等）一個能讀的轉介點，內容只是指向 `.claude/skills/` |
| Claude Code 入口 | `.claude/commands/` | Claude Code 寫死的目錄名 |
| Antigravity 入口 | `.agent/workflows/` | Antigravity 寫死的目錄名 |

**好處**：邏輯只寫一份在 `.claude/skills/`。哪天再加 Cursor、Cline、Windsurf，只要再開一個對應目錄丟薄殼指回 skills（或直接指 `prompts/` 薄殼），避免「Antigravity 版的 SOP 跟 Claude Code 版不一致」的維護地獄。

> **取捨**：`.agent/workflows/*.md` 雖然已直接指向 `.claude/skills/<name>/SKILL.md`，但 Antigravity 實際載入 SKILL.md 與否取決於該工具是否會跟著 markdown 連結展開讀檔。若不會，仍可透過 `prompts/<name>.md`（轉介薄殼）多一道間接。若未來需要讓 Antigravity 有完全獨立的 SOP 副本，要在 `.agent/workflows/` 補完整邏輯（目前不做）。

---

## 六、邏輯本體：`.claude/skills/` 與 `prompts/` 薄殼

三大操作的**完整 SOP 住在 `.claude/skills/`**。`prompts/` 從邏輯本體降級為薄殼，保留給 `.agent/workflows/` 與其他非 Claude Code 工具參考。

| 檔案 | 觸發語 | 做什麼 |
|------|--------|--------|
| `.claude/skills/ingest/SKILL.md` | 「處理 raw/X」「`/ingest raw/X`」 | 步驟 1–7：讀 → 與使用者對齊 → 寫 summary → 跨頁更新 entities/concepts → 更新 index/log → 跑機械檢查 → 回報 |
| `.claude/skills/query/SKILL.md` | 「問 X」「`/query X`」 | 步驟 1–5：讀 index → 讀相關頁 → 綜合作答（附引用）→ 問使用者要不要回填 syntheses → 補 open question |
| `.claude/skills/lint/SKILL.md` | 「lint wiki」「`/lint`」 | 階段 A（機械檢查 W1–W4）+ 階段 B（語意檢查 B1–B6 矛盾 / 孤兒 / 過期 / 缺頁 / 缺引用 / 研究題目）→ 產出 lint 報告 |
| `prompts/ingest.md` / `query.md` / `lint.md` | — | 薄殼：一段話指向 `.claude/skills/<name>/SKILL.md`。給 Antigravity 等非 Claude Code 工具讀。 |
| `.claude/Docs/prompts-conventions.md` | — | prompts 目錄本身的寫作規則（原 `prompts/AGENTS.md`，已搬至 Docs） |

---

## 七、`raw/` ── 原始 source【只讀】

| 路徑 | 用途 |
|------|------|
| `raw/.gitkeep` | 讓空目錄能進 git 版本控制。 |
| `.claude/Docs/raw-conventions.md` | **明確告訴 LLM：這個目錄只讀，絕對不可修改。** 把原始素材（PDF、書摘、訪談、文章、截圖）放這裡。（原 `raw/AGENTS.md`，已搬至 Docs） |

**核心紀律**：raw/ 是 source of truth，任何整理都要在 wiki/ 做，不可動 raw/。

---

## 八、`wiki/` ── LLM 維護的整理層

這是最核心的工作區。所有從 raw/ 整理出的內容都住這裡。

| 路徑 | 用途 |
|------|------|
| `.claude/Docs/wiki-conventions.md` | wiki 寫作核心公約：frontmatter 欄位、檔名規則（kebab-case 英文）、相對連結（不用 `[[wikilink]]`）、矛盾標註方式（`## ⚠️ Contradictions`）、不確定標註（`> [!unverified]`）。（原 `wiki/AGENTS.md`，已搬至 Docs） |
| `wiki/index.md` | **總索引**——每頁都要在這裡登記。`check-consistency.sh` 的 W1 就是查 index 與 wiki 實檔有沒有對齊。 |
| `wiki/log.md` | **更新日誌**——每次 ingest / query 回填 / lint 都要 append 一筆。W2 檢查格式。 |

### wiki 的四種頁面類型

| 子目錄 | 頁面類型 | 何時建頁 |
|--------|---------|---------|
| `wiki/entities/` | 實體頁：**人、組織、產品、模型**（具體的 X） | 看到一個有名字的東西，第一次出現就建 |
| `wiki/concepts/` | 概念頁：**術語、理論、方法**（抽象的 Y） | **跨多份 source 出現**才升級到獨立頁；第一次只在 summary 內提 |
| `wiki/summaries/` | 單一 source 的摘要頁（與 raw/ **一對一**對應） | 每 ingest 一份 source 就建一頁。W4 檢查所有 raw 都有 summary |
| `wiki/syntheses/` | 多源綜合頁 / 問答回填 / lint 報告 | query 操作的回填、lint 報告、跨多份 source 的主題綜合 |

各頁類型的專屬規則寫在 `.claude/Docs/wiki-entities.md` / `wiki-concepts.md` / `wiki-summaries.md` / `wiki-syntheses.md`。

### syntheses 內的特殊檔

| 檔案 | 用途 |
|------|------|
| `wiki/syntheses/open-questions.md` | 不確定 / 矛盾 / 待補事項的中央清單。任何頁上標 `> [!unverified]` 都該在這裡有對應條目。 |

---

## 九、`scripts/` ── 機械化檢查

| 檔案 | 用途 |
|------|------|
| `scripts/check-consistency.sh` | 一致性檢查腳本，跑四項：**W1** index ≡ wiki 實檔 / **W2** log.md 條目格式 / **W3** 內部相對連結未斷 / **W4** raw/ 每份 source 都有對應 summary |

這支腳本是**雙重防線**的核心：

```
本地：.githooks/pre-commit → 跑這支
                ↓ 通過才能 commit
                ↓
遠端：.github/workflows/consistency.yml → 同一支當合併門
```

---

## 十、自動化目錄（`.githooks/` 與 `.github/`）

| 路徑 | 用途 |
|------|------|
| `.githooks/pre-commit` | Git 本地鉤子：commit 前自動跑 `check-consistency.sh`，不通過就擋下。需要 `git config core.hooksPath .githooks` 啟用。 |
| `.github/workflows/consistency.yml` | GitHub Actions CI：PR / push 時跑同一支 `check-consistency.sh`，當合併門。 |

---

## 十一、實際走一次：典型工作流

### 場景 A：使用者放了一份新 PDF 進 raw/

```
使用者:  把 raw/2026-04-原子習慣-第三章.pdf ingest 進來
   ↓
[Claude Code]   讀 .claude/commands/ingest.md → 載入 .claude/skills/ingest/SKILL.md
[Antigravity]   讀 .agent/workflows/ingest.md → prompts/ingest.md（薄殼）→ 指向 SKILL.md
                                                ↓
                取得完整 SOP（邏輯本體在 .claude/skills/ingest/SKILL.md）
                ↓
                步驟 1: 讀 CLAUDE.md + .claude/Docs/wiki-conventions.md + wiki/index.md + source
                步驟 2: 與使用者對齊 TL;DR
                步驟 3: 寫 wiki/summaries/2026-04-原子習慣-第三章.md
                步驟 4: 跨頁更新 wiki/entities/、wiki/concepts/
                步驟 5: 更新 wiki/index.md + append wiki/log.md
                步驟 6: 跑 scripts/check-consistency.sh
                步驟 7: 回報使用者
```

### 場景 B：使用者問一個問題

```
使用者:  第三章與第七章對「習慣養成」的論述差在哪？
   ↓
                載入 .claude/skills/query/SKILL.md
                ↓
                讀 wiki/index.md → 找相關頁
                讀 wiki/entities/、concepts/、summaries/、syntheses/
                綜合作答（附引用）
                問使用者：要回填成 wiki/syntheses/ 一頁嗎？
                若同意 → 建頁 + 更新 index + log
```

### 場景 C：定期健康檢查

```
使用者:  lint wiki
   ↓
                載入 .claude/skills/lint/SKILL.md
                ↓
                階段 A：跑 scripts/check-consistency.sh（W1–W4）
                階段 B：語意檢查 B1 矛盾 / B2 孤兒 / B3 過期 / B4 缺頁 / B5 缺引用 / B6 研究題目
                產出 wiki/syntheses/lint-YYYY-MM-DD.md
                **不直接動手修**——讓使用者決定
```

---

## 十二、設計核心思想速覽

1. **三層分離**：raw / wiki / 規則，職責不混。
2. **薄殼指向本體**：`.claude/commands/`、`.agent/workflows/`、`prompts/` 都是薄殼，邏輯只寫一份在 `.claude/skills/`，多 agent 工具共存無痛。
3. **漸進式披露**：根 `CLAUDE.md` 精簡（< 100 行），細節下放到 `.claude/Docs/` 下各規定檔，模型按需讀。
4. **機械 + 語意雙檢**：能用腳本檢的事不靠人腦（W1–W4），需要判斷的事不靠腳本（B1–B6）。
5. **雙重合併門**：本地 pre-commit + 遠端 CI 跑同一支腳本，避免漏網。
6. **診斷 ≠ 治療**：lint 只報告、不亂改；query 寫綜合前先問使用者要不要回填。**人類掌舵，AI 執行。**
